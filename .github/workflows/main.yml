name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Build Docker image
      run: |
        docker buildx create --use
        docker buildx build --tag hemanthad23/portfolio_testing --push .

    env:
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}  # Use the Docker Hub access token secret you created
      DOCKER_USERNAME: hemanthad23  # Your Docker Hub username
